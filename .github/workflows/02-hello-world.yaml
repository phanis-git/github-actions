name: 02 - Hello World Workflow

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "02 - Hello World from GitHub Actions 🚀"
